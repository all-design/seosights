/**
 * Landing footer — re-exports the shared SiteFooter so the homepage and all
 * inner pages share the same rich multi-column footer with links to every
 * top-level route (Free Tools, Pricing, Blog, Affiliates, …).
 */
export { default } from '@/components/site/SiteFooter'
